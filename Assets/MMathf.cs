using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class MMathf
{
    // Start is called before the first frame update
    public static float Powed(float i,float j)
    {
        return Mathf.Pow(i, j);
    }
}
